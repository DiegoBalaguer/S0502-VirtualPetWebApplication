<configuration>

<!-- comentamos la parte de salida en consola-->

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender para archivo -->
    <appender name="ROLLING" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>_local/logs/VirtualGameApp.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- archivo nuevo cada día -->
            <fileNamePattern>_local/logs/VirtualGameApp.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>4</maxHistory> <!-- conservar 4 días de logs -->
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- configuración para los mensajes de cheking status de MongoDB -->
<!--
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="org.mongodb.driver" level="WARN"/>
    <logger name="org.mongodb.driver.cluster" level="WARN"/>
    <logger name="org.mongodb.driver.connection" level="WARN"/>
    <logger name="org.mongodb.driver.management" level="WARN"/>
-->
    <root level="debug">
        <appender-ref ref="CONSOLE"/>
        <!-- <appender-ref ref="ROLLING"/> -->
        <!-- <appender-ref ref="STDOUT" /> -->
    </root>
</configuration>
